# 液态智能网页开发计划

> 当前实现范围（已确认）：用户一次提交完整 Query，不做追加对话。系统先策划七个内容侧重点不同的独立网页方案，卡片标题、人群、重点和章节随 Query 生成。点击方案后单独生成其正文；视觉布局服务于内容，不以七套皮肤代替七个方案。已生成正文缓存。下文的持续修改、版本撤销等属于后续设想，不在本次范围。

版本：v0.1  
日期：2026-09-04  
状态：规划阶段，尚未开始实现

## 1. 产品定位

构建一个通用的“任务到界面”生成系统：将用户的自然语言意图，转化成适合当前任务的交互网页，并在用户继续表达需求或操作页面时持续更新。

用户提供的车载冰箱推荐视频是体验样本，不构成产品的领域边界。系统不限于商品推荐，也不限于某一行业。

“液态”指内容结构、布局与交互随任务变化，而不是仅切换主题、颜色或动画。

### 典型任务

| 用户 Query | 适合的任务界面 |
|---|---|
| 帮我理解傅里叶变换 | 解释、波形演示、可调参数 |
| 安排一次带老人孩子的旅行 | 行程、地图、预算、调整入口 |
| 分析这份销售数据 | 图表、筛选、异常分析、明细 |
| 比较这几种技术方案 | 对比矩阵、约束、取舍分析 |
| 帮我规划一个项目 | 里程碑、任务依赖、资源分配 |
| 帮我计算装修预算 | 可编辑清单、计算器、费用分布 |
| 帮我选择合适的车载冰箱 | 条件确认、产品比较、来源与适配说明 |

以上示例用于检验通用性，不是封闭的任务分类。对于一句话即可解决的问题，系统应允许直接回答，不强制生成复杂网页。

## 2. 核心闭环

```text
Query + 用户资料 + 当前上下文
                ↓
       理解任务目标与约束
                ↓
 决定所需信息、计算、操作与澄清
                ↓
       获取数据、执行计算
                ↓
      生成内容结构与交互界面
                ↓
 用户操作页面或继续用自然语言修改
                ↓
       更新任务状态与界面
```

任务规划和界面规划分别处理：

- 任务规划：为了完成用户目标，需要做什么。
- 界面规划：如何组织信息和操作，帮助用户完成任务。

不将所有任务统一套成“标题、卡片、表格”的固定页面。

## 3. 系统架构

| 模块 | 职责 |
|---|---|
| 意图理解层 | 识别目标、约束、输入资料、预期产出与缺失信息 |
| 任务规划层 | 分解步骤，判断是否需要搜索、文件读取、计算或澄清 |
| 工具与数据层 | 接入搜索、文件解析、计算及外部服务，保留数据来源 |
| 内容模型层 | 管理事实、推断、计算结果及其关联，供界面使用 |
| 界面生成层 | 决定布局、组件、信息层级与交互方式 |
| 状态管理层 | 保留输入、选择、编辑、操作结果及任务进度 |
| 验证层 | 检查事实依据、计算、页面配置与交互是否成立 |

任务状态独立于页面布局。切换图表、重组页面或继续生成界面时，不应丢失用户已经输入的数据与选择。

## 4. 三个核心协议

以下为协议设计范围，具体字段和类型在第一阶段确定。

### 4.1 任务描述协议

表达用户目标、硬性约束、偏好、输入资料、预期产出、未知项、假设、计划步骤与完成条件。支持复合任务，不依赖单一领域标签。

验收重点：不同领域的 Query 能用同一协议表达；关键歧义显式记录；必要时进行澄清。

### 4.2 页面配置协议

表达页面模块、信息层级、布局、数据绑定、交互动作、加载与错误状态，以及内容来源。组件选择应服务于任务，而不是为了填满页面。

验收重点：配置可校验；组件的数据和动作可追踪；无法渲染时有明确回退。

### 4.3 状态更新协议

表达自然语言修改与页面操作产生的事件、受影响的数据、需要重新执行的步骤和页面局部更新。支持版本、撤销与失败恢复。

验收重点：无关条件保持不变；关联结果同步更新；重组页面不丢失工作；连续操作结果一致。

## 5. 界面生成策略

### 标准组件组合

使用表格、图表、地图、时间轴、表单、对比矩阵、计算器等通用组件，覆盖常见的信息表达与操作需求。

组件库提供通用能力，不为每个行业预制固定页面。每个模块都应明确帮助用户理解什么或完成什么操作。

### 定制界面生成

对于标准组件无法表达的任务，如交互教学演示和特殊模拟器，使用隔离运行的定制界面。

定制界面需要运行检查、数据访问限制、错误处理与失败回退。不得将未经校验的生成代码直接作为可信运行逻辑。

### 数据与逻辑原则

- 区分外部事实、用户提供信息、模型推断和计算结果。
- 关键事实保留来源与更新时间；未知信息不编造。
- 可以确定计算的结果由计算工具或程序产生。
- 信息不足时，根据对任务结果的影响决定追问或展示假设。
- 第一版不默认具备外部提交、购买或其他有实际后果的执行能力。

## 6. 分阶段开发

| 阶段 | 核心工作 | 交付结果 |
|---|---|---|
| ① 任务定义与评测集 | 收集跨领域 Query；设计三个核心协议；定义完成条件与评分标准 | 任务样本、协议草案、验收标准 |
| ② 通用生成闭环 | 实现意图解析、任务规划、页面配置及标准组件渲染 | 同一入口生成多类可操作页面 |
| ③ 数据与工具接入 | 接入文件、搜索与计算；建立来源追踪和异常处理 | 使用真实资料与计算结果完成任务 |
| ④ 连续交互 | 实现自然语言修改、页面操作、局部更新、状态保留与撤销 | 页面随任务推进持续变化 |
| ⑤ 定制界面生成 | 加入隔离运行、校验、错误修复与回退 | 支持超出标准组件的交互需求 |
| ⑥ 对照测试与优化 | 与文本回答、搜索结果及固定界面比较 | 明确实际收益、失败类型与适用边界 |

阶段②使用固定测试数据验证闭环；真实信息获取能力在阶段③接入。先验证通用能力，再扩展工具数量和视觉风格。

## 7. MVP 范围

### 必须包含

- 统一的自然语言入口。
- 至少覆盖解释、分析、规划、比较、计算五类任务。
- 结构化任务描述与经过校验的页面配置。
- 一组可复用的交互组件。
- 基础文件、搜索和计算能力，具体接入范围在实现前确定。
- 来源展示、缺失信息与错误状态。
- 自然语言修改、页面操作及连续状态更新。
- 跨领域评测和基线对照。

### 后续扩展

- 更丰富的文件格式和外部服务。
- 更复杂的定制界面与模拟器。
- 更多视觉风格。
- 多用户协作、长期任务和外部执行能力。

第一版可以限制工具、文件格式与操作范围，但不把用户需求限定为单一品类。不承诺处理所有 Query；超出能力范围时明确说明。

## 8. 验证计划

### 评测样本

覆盖五类基础任务，以及复合任务、模糊需求、条件冲突、数据缺失、工具失败和连续修改。加入未用于开发的 Query，检验通用性。

### 核心指标

| 维度 | 检查问题 |
|---|---|
| 目标理解 | 是否理解真正目标、硬性条件和预期产出？ |
| 任务完成 | 用户能否完成任务，结果是否满足约束？ |
| 表达适配 | 页面形式是否有助于任务，而非仅增加装饰？ |
| 信息可靠 | 事实是否有依据，计算能否复核，未知项是否明确？ |
| 交互有效 | 操作和参数变化是否引起正确的关联更新？ |
| 状态连续 | 连续修改或页面重组后，已有工作是否保留？ |
| 效率 | 完成任务时间、操作量、生成延迟与成本是否合理？ |

### 对照实验

1. 表达对照：保持内容、数据和结果一致，对比文本、固定界面与动态界面的理解正确率和任务完成时间。
2. 系统对照：使用相同需求与可比的数据条件，对比完整系统与搜索或传统工具的任务完成效果。
3. 连续修改测试：只改变一个条件，检查相关结果是否更新、无关条件是否保留。
4. 泛化测试：使用新的领域与表达方式，检查是否仍能选择适当工具和界面。

按任务类型分别报告结果，不用单一平均分掩盖失败。主观满意度作为辅助指标，不能代替准确性与任务完成率。

### 第一版验收原则

- 同一个系统能够完成五类基础任务。
- 已知硬性约束不被静默忽略。
- 关键事实可追溯，计算可复核，未知信息不编造。
- 关键交互真实有效，不使用看似可操作的装饰控件。
- 连续修改保留未变条件，重组布局保留任务状态。
- 不支持的任务或工具失败有明确说明和可恢复路径。
- 相较基线，先保证结果正确，再验证效率与理解收益。

具体数值门槛在评测集和基线确定后、正式对照测试前设定。

## 9. 主要风险及处理方向

| 风险 | 处理方向 |
|---|---|
| 页面漂亮但任务没有完成 | 以任务完成条件验收，独立评估视觉和准确性 |
| 模型生成错误事实或计算 | 来源追踪、工具计算、显式区分推断与事实 |
| 通用生成退化成固定模板 | 用跨领域与未见样本检验模块选择和交互适配 |
| 自由生成界面不稳定 | 标准组件优先，定制界面隔离、校验和回退 |
| 修改需求后状态丢失或不一致 | 独立状态模型、明确依赖、版本与撤销机制 |
| 页面过度复杂 | 根据任务必要性决定模块数量，允许简短回答 |
| 生成时间和成本过高 | 局部更新、复用有效结果，测量各阶段耗时与成本 |

## 10. 下一步

先完成阶段①：形成跨领域 Query 样本、三个核心协议草案与验收标准，再据此确定技术栈和实现范围。

本计划未选定具体模型、框架、托管服务或外部供应商，也未设定工期。这些选择应在协议和最小闭环明确后确定。
